import { SonSwiperDirective } from './son-swiper.directive';

describe('SonSwiperDirective', () => {
  it('should create an instance', () => {
    const directive = new SonSwiperDirective();
    expect(directive).toBeTruthy();
  });
});
